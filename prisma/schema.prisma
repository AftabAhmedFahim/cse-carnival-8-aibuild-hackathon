// Prisma schema — datasource + generator only at this stage.
// Models land in MILESTONE 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}
